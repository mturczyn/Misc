using System;

namespace CSharp8._0NewFeatures
{
  /* PLAN:
   * PatternMatchingAndTupleAssignment:
   * - readonly structs
   * - tuples: assignment,
   *           constructors, property only assignment
   * - pattern matching,
   * IndexesRangesNewSyntax:
   * - klasy Index i Range plus nowa składnia,
   */
  class Program
  {
    static void Main(string[] args)
    {
      IndexesRangesNewSyntax.Demo();
    }
  }
}
